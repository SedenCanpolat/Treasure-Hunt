using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TaskScriptNames : MonoBehaviour
{ 
    public MonoBehaviour[] scriptNames;
    public ExitHome exitHome;
    public Cleaning cleaning;

    
    // Start is called before the first frame update
    void Start()
    {
       // scriptNames[0] = exitHome;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
